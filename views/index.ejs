<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs')  %>
</head>

<header>
    <h1><%= Exercise[0].muscleGroup %> Exercises </h1>
    <nav>
        <a href="/">Home</a>
        <a href="<%= Exercise[0].muscleGroup %>/new ">New</a>
    </nav>
</header>

<body>
    <% Exercise.forEach(ex => {%>
    <div>
        <h1>Exercise: <%= ex.exercise %> </h1>
        <p>Sets: <%= ex.sets %></p>
        <p>Reps: <%= ex.reps %></p>
        <p>RPE: <%= ex.rpe %></p>
        <p>Rest: <%= ex.rest %></p>
        <p>Weight: <%= ex.weight %> </p>
        <p>Notes: <%= ex.notes %> </p>
        <p>Completed: <%= ex.completed %> </p>
        <p>Date: <%= ex.createdAt %> </p>
        <a href="/<%= ex.muscleGroup %>/<%= ex.id %>">View</a>
        <form action="/<%= ex.muscleGroup %>/<%= ex.id %>?_method=DELETE" method="POST">
            <input type="submit" value="Delete">
        </form>
    </div>
    <%});  %>

</body>

</html>